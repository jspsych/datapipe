{
  "compilerOptions": {
    "target": "ES2018", // Compile to ES6
    "module": "es2020", // ESM emit; es2020 (superset of ES6) also allows dynamic import(). Nothing uses it now -- mail-delivery.ts lazy-loaded the AWS SDK off the hot path until delivery moved to Resend, which needs no SDK at all
    "outDir": "./lib", // Output compiled files to the lib directory
    "rootDir": "./src", // Specify the root directory of input files
    "moduleResolution": "node", // Use Node.js module resolution mechanism
    "sourceMap": true, // Generate corresponding '.map' file
    "esModuleInterop": true, // Enables compatibility with non-ES module spec compliant imports
    "strict": true, // Enable all strict type-checking options
    "skipLibCheck": true // Skip type checking of declaration files
  },
  "include": ["src/**/*"], // Include all files in the src directory
  "exclude": ["node_modules", "**/*.spec.ts"], // Exclude node_modules and test files
  "compileOnSave": true
}
